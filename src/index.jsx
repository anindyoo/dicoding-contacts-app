import React from 'react';
import { createRoot } from 'react-dom/client';
 
const element = <h1>BUCKIIN', LIKE A MECHANICAL BULL.</h1>;
 
const root = createRoot(document.getElementById('root'));
root.render(element);
